# Models

`current/`, yalnız açıkça yayınlanmış aktif model kopyalarını içerir.

Eğitim run'larının asıl çıktıları `runs/` altında kalır. Fine-tune sırasında
`--publish-root-model` verilmedikçe bu klasördeki modeller değiştirilmez.
